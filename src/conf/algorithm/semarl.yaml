# @package algorithm
# SEMARL shares every ERL parameter 
defaults:
  - erl
  - _self_

name: semarl

# The bootstrap horizon stays at erl.yaml's h_steps
p_surr_min: 0.0
p_surr_max: 0.9
p_beta: 3.0
td_ema_decay: 0.1
